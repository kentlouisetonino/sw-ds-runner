#include <iostream>
using namespace std;

/*
 * This function adds a new line based
 * on the number of new lines needed.
 *
 * */
void new_lines(int number) {
  for (int i = 0; i < number; i++) {
    cout << endl;
  }
}
